
#include<iostream>
using namespace std;
class node{
    public:
    int data ;
    node* next ;

    node(int data){
        this -> data = data ;
        next = NULL ;
    }
};
node *takeInput(){
    node *head = NULL ;
    int data ;
    cin >> data ;
    while(data != -1){
        if(head == NULL){
            node *newNode = new node(data) ;
            head = newNode ;
        }
        else{
            node *temp = head ;
            node * newNode = new node(data) ;
            while(temp -> next != NULL){
                temp = temp -> next ;
            }
            temp -> next = newNode ;
        }
        cin >> data ;
    }
    return head ;
}
void print(node *head){
    node * temp = head ;
    while(temp != NULL){
        cout << temp -> data <<" ";
        temp = temp -> next ;
    }

}
int main(){
    node *head = takeInput() ;
    print(head) ;
}
